import streamlit as st

st.set_page_config(page_title="내가 선호하는 국내 여행지는?", page_icon="🧳", layout="centered")

st.title("내가 선호하는 국내 여행지는?")
st.caption("간단한 선호도 조사 3문항으로 당신에게 어울리는 국내 여행 스타일을 알아봐요!")

# 질문 1
q1 = st.radio(
    "질문 1: 여행 목적은 무엇인가요?",
    ["힐링", "휴양", "액티비티", "관광"],
    index=None,
    key="q1",
)

# 질문 2
q2 = st.radio(
    "질문 2: 여행의 동반자는 누구인가요?",
    ["혼자", "연인", "가족", "친구"],
    index=None,
    key="q2",
)

# 질문 3
q3 = st.radio(
    "질문 3: 이동수단은 어떻게 되나요?",
    ["고속버스", "기차", "자동차", "비행기"],
    index=None,
    key="q3",
)

st.divider()

# 결과 보기 버튼
if st.button("결과 보기", type="primary"):
    if q1 is None or q2 is None or q3 is None:
        st.warning("모든 질문에 답해야 결과를 볼 수 있어요!")
    else:
        st.write("분석 중...")  # 다음 단계에서 추천/연동 기능 추가
